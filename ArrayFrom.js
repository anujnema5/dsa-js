const array = {
    a: "Apple",
    b: "Banana",
    c: "Cipher"
}
// console.log(Array.from(array, (array, i) => array.includes("Apple")));

// console.log(Array.from(array, (array) => array));

console.log(Array.from(array));